{
  "name": "nRF Connect SDK DevContainer",
  "image": "ghcr.io/nrfconnect/sdk-nrf-toolchain:v2.6.99",  // Use the specific container image


  "postCreateCommand": "apt-get update && apt-get install -y build-essential git && west init -l . && west update"
}